<%= form_for(@doctor_visit) do |f| %>
  <% if @doctor_visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctor_visit.errors.count, "error") %> prohibited this doctor_visit from being saved:</h2>

      <ul>
      <% @doctor_visit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :for %><br />
    <%= f.text_field :for %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
